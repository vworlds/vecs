{
  "name": "@vworlds/vecs-demo-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --conditions=development --include ../../lib src/main.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\" --no-error-on-unmatched-pattern",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@vworlds/vecs": "*",
    "@vworlds/vecs-server": "*",
    "@vworlds/vecs-wire": "*",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "tsx": "^4.20.6"
  },
  "license": "UNLICENSED"
}
